<%= form_for(@hscore) do |f| %>
  <% if @hscore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hscore.errors.count, "error") %> prohibited this hscore from being saved:</h2>

      <ul>
      <% @hscore.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game %><br>
    <%= f.text_field :game %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
